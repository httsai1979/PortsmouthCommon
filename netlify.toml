[build]
  command = "npm run build"
  publish = "dist"

# 解決 React/Vite 單頁應用 (SPA) 重新整理後 404 的問題
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 設定快取策略 (選擇性)
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"